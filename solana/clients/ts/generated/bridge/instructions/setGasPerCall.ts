/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BRIDGE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_GAS_PER_CALL_DISCRIMINATOR = new Uint8Array([
  164, 95, 213, 130, 26, 69, 82, 127,
]);

export function getSetGasPerCallDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_GAS_PER_CALL_DISCRIMINATOR
  );
}

export type SetGasPerCallInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountBridge extends string | IAccountMeta<string> = string,
  TAccountGuardian extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountBridge extends string
        ? WritableAccount<TAccountBridge>
        : TAccountBridge,
      TAccountGuardian extends string
        ? ReadonlySignerAccount<TAccountGuardian> &
            IAccountSignerMeta<TAccountGuardian>
        : TAccountGuardian,
      ...TRemainingAccounts,
    ]
  >;

export type SetGasPerCallInstructionData = {
  discriminator: ReadonlyUint8Array;
  newVal: bigint;
};

export type SetGasPerCallInstructionDataArgs = { newVal: number | bigint };

export function getSetGasPerCallInstructionDataEncoder(): Encoder<SetGasPerCallInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newVal', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_GAS_PER_CALL_DISCRIMINATOR })
  );
}

export function getSetGasPerCallInstructionDataDecoder(): Decoder<SetGasPerCallInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newVal', getU64Decoder()],
  ]);
}

export function getSetGasPerCallInstructionDataCodec(): Codec<
  SetGasPerCallInstructionDataArgs,
  SetGasPerCallInstructionData
> {
  return combineCodec(
    getSetGasPerCallInstructionDataEncoder(),
    getSetGasPerCallInstructionDataDecoder()
  );
}

export type SetGasPerCallInput<
  TAccountBridge extends string = string,
  TAccountGuardian extends string = string,
> = {
  /** The bridge account containing configuration */
  bridge: Address<TAccountBridge>;
  /** The guardian account authorized to update configuration */
  guardian: TransactionSigner<TAccountGuardian>;
  newVal: SetGasPerCallInstructionDataArgs['newVal'];
};

export function getSetGasPerCallInstruction<
  TAccountBridge extends string,
  TAccountGuardian extends string,
  TProgramAddress extends Address = typeof BRIDGE_PROGRAM_ADDRESS,
>(
  input: SetGasPerCallInput<TAccountBridge, TAccountGuardian>,
  config?: { programAddress?: TProgramAddress }
): SetGasPerCallInstruction<TProgramAddress, TAccountBridge, TAccountGuardian> {
  // Program address.
  const programAddress = config?.programAddress ?? BRIDGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    bridge: { value: input.bridge ?? null, isWritable: true },
    guardian: { value: input.guardian ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.bridge),
      getAccountMeta(accounts.guardian),
    ],
    programAddress,
    data: getSetGasPerCallInstructionDataEncoder().encode(
      args as SetGasPerCallInstructionDataArgs
    ),
  } as SetGasPerCallInstruction<
    TProgramAddress,
    TAccountBridge,
    TAccountGuardian
  >;

  return instruction;
}

export type ParsedSetGasPerCallInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The bridge account containing configuration */
    bridge: TAccountMetas[0];
    /** The guardian account authorized to update configuration */
    guardian: TAccountMetas[1];
  };
  data: SetGasPerCallInstructionData;
};

export function parseSetGasPerCallInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedSetGasPerCallInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      bridge: getNextAccount(),
      guardian: getNextAccount(),
    },
    data: getSetGasPerCallInstructionDataDecoder().decode(instruction.data),
  };
}
