/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BRIDGE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SET_MINIMUM_BASE_FEE_DISCRIMINATOR = new Uint8Array([
  56, 95, 58, 94, 221, 136, 138, 156,
]);

export function getSetMinimumBaseFeeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_MINIMUM_BASE_FEE_DISCRIMINATOR
  );
}

export type SetMinimumBaseFeeInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountBridge extends string | IAccountMeta<string> = string,
  TAccountGuardian extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountBridge extends string
        ? WritableAccount<TAccountBridge>
        : TAccountBridge,
      TAccountGuardian extends string
        ? ReadonlySignerAccount<TAccountGuardian> &
            IAccountSignerMeta<TAccountGuardian>
        : TAccountGuardian,
      ...TRemainingAccounts,
    ]
  >;

export type SetMinimumBaseFeeInstructionData = {
  discriminator: ReadonlyUint8Array;
  newFee: bigint;
};

export type SetMinimumBaseFeeInstructionDataArgs = { newFee: number | bigint };

export function getSetMinimumBaseFeeInstructionDataEncoder(): Encoder<SetMinimumBaseFeeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newFee', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_MINIMUM_BASE_FEE_DISCRIMINATOR })
  );
}

export function getSetMinimumBaseFeeInstructionDataDecoder(): Decoder<SetMinimumBaseFeeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newFee', getU64Decoder()],
  ]);
}

export function getSetMinimumBaseFeeInstructionDataCodec(): Codec<
  SetMinimumBaseFeeInstructionDataArgs,
  SetMinimumBaseFeeInstructionData
> {
  return combineCodec(
    getSetMinimumBaseFeeInstructionDataEncoder(),
    getSetMinimumBaseFeeInstructionDataDecoder()
  );
}

export type SetMinimumBaseFeeInput<
  TAccountBridge extends string = string,
  TAccountGuardian extends string = string,
> = {
  /** The bridge account containing configuration */
  bridge: Address<TAccountBridge>;
  /** The guardian account authorized to update configuration */
  guardian: TransactionSigner<TAccountGuardian>;
  newFee: SetMinimumBaseFeeInstructionDataArgs['newFee'];
};

export function getSetMinimumBaseFeeInstruction<
  TAccountBridge extends string,
  TAccountGuardian extends string,
  TProgramAddress extends Address = typeof BRIDGE_PROGRAM_ADDRESS,
>(
  input: SetMinimumBaseFeeInput<TAccountBridge, TAccountGuardian>,
  config?: { programAddress?: TProgramAddress }
): SetMinimumBaseFeeInstruction<
  TProgramAddress,
  TAccountBridge,
  TAccountGuardian
> {
  // Program address.
  const programAddress = config?.programAddress ?? BRIDGE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    bridge: { value: input.bridge ?? null, isWritable: true },
    guardian: { value: input.guardian ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.bridge),
      getAccountMeta(accounts.guardian),
    ],
    programAddress,
    data: getSetMinimumBaseFeeInstructionDataEncoder().encode(
      args as SetMinimumBaseFeeInstructionDataArgs
    ),
  } as SetMinimumBaseFeeInstruction<
    TProgramAddress,
    TAccountBridge,
    TAccountGuardian
  >;

  return instruction;
}

export type ParsedSetMinimumBaseFeeInstruction<
  TProgram extends string = typeof BRIDGE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The bridge account containing configuration */
    bridge: TAccountMetas[0];
    /** The guardian account authorized to update configuration */
    guardian: TAccountMetas[1];
  };
  data: SetMinimumBaseFeeInstructionData;
};

export function parseSetMinimumBaseFeeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedSetMinimumBaseFeeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      bridge: getNextAccount(),
      guardian: getNextAccount(),
    },
    data: getSetMinimumBaseFeeInstructionDataDecoder().decode(instruction.data),
  };
}
